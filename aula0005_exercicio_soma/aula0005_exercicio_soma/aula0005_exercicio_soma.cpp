

#include <iostream>
#include <string>
#include <locale>
#include <math.h>
using std::cout; using std::endl; using std::getline;
using std::cin; using std::string; using std::setlocale;



int main()
{
    setlocale(LC_ALL, "pt_BR.UTF-8");
    cout << "#############PROGRAMA DE MATEMATICA################\n";
    int x = 18;
    int y = 9;
    int soma = x + y;
    cout << "A soma de " << x << " + " << y << " = " << soma << "\n"
    
}

